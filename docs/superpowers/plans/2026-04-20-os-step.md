# OpenSubtitles — Step dedicato + bug fix — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Aggiungere step dedicato OS prima della tabella tracce in Sync (step 5 su 7) e Mux (step 3 su 5), correggere bug `[object Object]` su mux con SRT scaricato da OS, e rendere il modale risultati persistente per download multipli.

**Architecture:** Fix backend puro (default mkvmerge_id), refactor HTML wizard (nuovi step, breadcrumb), refactor JS (goToStep/mxGoStep, funzioni OS step, modale persistente). Nessuna modifica al backend OS (subtitle_downloader.py, /api/subtitles/*).

**Tech Stack:** Python/Pydantic (main.py), HTML vanilla, JS vanilla, unittest

---

## File Structure

| File | Modifiche |
|------|-----------|
| `app/main.py` | `mkvmerge_id: int = -1` default in `SimpleMuxTrack` |
| `app/static/index.html` | Step 5 OS Sync, step 3 OS Mux; breadcrumb 7/5 pill; rimozione collassabili OS; modale counter + Chiudi; rinomina step5→6→7 Sync, step3→4 step4→5 Mux |
| `app/static/app.js` | i18n keys; `goToStep` logica step 6/7; `mxGoStep` logica step 4/5; funzioni OS step Sync/Mux; modale persistente multi-download; `ffprobe_index` univoco |
| `tests/test_unit.py` | Test `mkvmerge_id` default in `SimpleMuxTrack` |

---

## Task 1: Backend — mkvmerge_id default

**Files:**
- Modify: `app/main.py:327`
- Test: `tests/test_unit.py`

- [ ] **Step 1.1: Scrivi il test che fallisce**

Aggiungi in `tests/test_unit.py`, nella classe `TestBuildMkvmergeCmd` (o crea una nuova classe `TestSimpleMuxTrack` in fondo al file):

```python
class TestSimpleMuxTrack(unittest.TestCase):

    def test_mkvmerge_id_has_default(self):
        """SimpleMuxTrack deve poter essere creata senza mkvmerge_id (tracce OS standalone)."""
        import sys
        sys.path.insert(0, str(Path(__file__).parent.parent / "app"))
        from main import SimpleMuxTrack
        track = SimpleMuxTrack(
            source_file_idx=0,
            type="subtitle",
            codec="SRT",
        )
        self.assertEqual(track.mkvmerge_id, -1)

    def test_mkvmerge_id_explicit_value(self):
        """mkvmerge_id esplicito deve essere preservato."""
        from main import SimpleMuxTrack
        track = SimpleMuxTrack(
            source_file_idx=0,
            mkvmerge_id=5,
            type="subtitle",
            codec="SRT",
        )
        self.assertEqual(track.mkvmerge_id, 5)
```

- [ ] **Step 1.2: Esegui il test per verificare che fallisce**

```bash
cd /media/devmon/Esterno1/Samba/mkv_maximus
python -m unittest tests.test_unit.TestSimpleMuxTrack -v
```

Atteso: `FAIL` — `TypeError: __init__() missing 1 required positional argument: 'mkvmerge_id'`

- [ ] **Step 1.3: Applica il fix**

In `app/main.py`, riga 316, cambia:

```python
    mkvmerge_id: int
```

in:

```python
    mkvmerge_id: int = -1
```

- [ ] **Step 1.4: Esegui i test per verificare che passano**

```bash
python -m unittest tests/test_unit.py -v 2>&1 | tail -5
```

Atteso: tutti i test passano, incluso `TestSimpleMuxTrack`.

- [ ] **Step 1.5: Commit**

```bash
git add app/main.py tests/test_unit.py
git commit -m "fix(mux): mkvmerge_id default=-1 in SimpleMuxTrack (BUG-OS-MULTI)

Tracce OS standalone non includono mkvmerge_id -> Pydantic 422 -> [object Object].
Con default=-1 la validazione passa; build_mkvmerge_cmd_multi non usa il campo
per tracce con converted_path.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Task 2: i18n — nuove chiavi di traduzione

**Files:**
- Modify: `app/static/app.js` (sezione i18n ITA e ENG, righe ~230 e ~610)

- [ ] **Step 2.1: Aggiungi le nuove chiavi i18n ITA**

Nella sezione `const TRANSLATIONS = { it: {` (intorno alla riga 230 di app.js), aggiungi le seguenti chiavi prima della chiusura `}`:

```js
    // OS step
    step_os: 'OS',
    mux_step_os: 'OS',
    os_step_title: '🌐 Scarica sottotitoli da OpenSubtitles',
    os_step_lang_label: 'Lingua:',
    os_step_file_label: 'Da file:',
    os_step_search_btn: 'Cerca',
    os_step_skip: 'Avanti senza scaricare →',
    os_step_season_warn: 'In modalità stagione i sottotitoli OS si aggiungono per singolo episodio dopo il batch.',
    os_added_counter: 'sottotitolo/i aggiunto/i',
    js_add: 'Aggiungi',
    js_added: '✓ Aggiunto',
    os_modal_close: 'Chiudi',
```

- [ ] **Step 2.2: Aggiungi le nuove chiavi i18n ENG**

Nella sezione `en: {` (intorno alla riga 610 di app.js), aggiungi le stesse chiavi in inglese prima della chiusura `}`:

```js
    // OS step
    step_os: 'OS',
    mux_step_os: 'OS',
    os_step_title: '🌐 Download subtitles from OpenSubtitles',
    os_step_lang_label: 'Language:',
    os_step_file_label: 'From file:',
    os_step_search_btn: 'Search',
    os_step_skip: 'Skip and continue →',
    os_step_season_warn: 'In season mode, OS subtitles must be added per episode after the batch.',
    os_added_counter: 'subtitle(s) added',
    js_add: 'Add',
    js_added: '✓ Added',
    os_modal_close: 'Close',
```

- [ ] **Step 2.3: Commit**

```bash
git add app/static/app.js
git commit -m "feat(i18n): nuove chiavi traduzione step OS (ITA+ENG)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Task 3: Modale OS — persistente per multi-download

**Files:**
- Modify: `app/static/index.html` (modale `osSearchModal`, righe ~1041-1049)
- Modify: `app/static/app.js` (funzioni render risultati + confirm)

- [ ] **Step 3.1: Aggiorna HTML modale**

In `index.html`, sostituisci l'intera sezione `<!-- ── OpenSubtitles search results modal -->`:

```html
<!-- ── OpenSubtitles search results modal ───────────────────────────────── -->
<dialog id="osSearchModal" class="info-modal">
  <div class="info-modal-inner" style="max-width:640px;width:92vw">
    <div class="info-modal-header">
      <div class="info-modal-title" data-i18n="os_modal_title">🔍 Risultati OpenSubtitles</div>
      <button class="modal-close-btn" onclick="document.getElementById('osSearchModal').close()">✕</button>
    </div>
    <div id="osAddedCounter" style="display:none;padding:0.4rem 1rem;font-size:0.82rem;
         color:var(--success);border-bottom:1px solid var(--border)"></div>
    <div id="osSearchBody" style="padding:0.75rem 1rem;max-height:55vh;overflow-y:auto"></div>
    <div style="padding:0.5rem 1rem;text-align:right;border-top:1px solid var(--border)">
      <button class="btn btn-ghost btn-sm"
              onclick="document.getElementById('osSearchModal').close()"
              data-i18n="os_modal_close">Chiudi</button>
    </div>
  </div>
</dialog>
```

- [ ] **Step 3.2: Aggiungi variabile globale contatore**

In `app.js`, vicino alle altre variabili globali (`let _osPendingSearch`, cerca riga ~4556), aggiungi:

```js
let _osAddedCount = 0;
```

- [ ] **Step 3.3: Aggiungi funzione helper per aprire il modale**

In `app.js`, dopo la riga `let _osAddedCount = 0;`, aggiungi:

```js
function _osOpenModal() {
  _osAddedCount = 0;
  const ctr = document.getElementById('osAddedCounter');
  if (ctr) { ctr.style.display = 'none'; ctr.textContent = ''; }
  document.getElementById('osSearchModal').showModal();
}

function _osIncrementCounter() {
  _osAddedCount++;
  const ctr = document.getElementById('osAddedCounter');
  if (ctr) {
    ctr.textContent = `${_osAddedCount} ${t('os_added_counter')}`;
    ctr.style.display = 'block';
  }
}
```

- [ ] **Step 3.4: Aggiorna osStandaloneShowResults (Sync) — modale persistente**

Trova la funzione `osStandaloneShowResults` in `app.js` (riga ~4594) e sostituiscila interamente:

```js
function osStandaloneShowResults(results, hash, apiTotal, lang) {
  const body = document.getElementById('osSearchBody');
  if (results.length === 0) {
    const hashInfo = hash ? `<br><small style="font-family:monospace;opacity:.7">hash: ${hash} · api_total: ${apiTotal ?? '?'}</small>` : '';
    body.innerHTML = `<p style="color:var(--text-muted);text-align:center;padding:1rem">${t('js_os_no_results')}${hashInfo}</p>`;
    _osOpenModal();
    return;
  }
  body.innerHTML = results.map(res => `
    <div style="border-bottom:1px solid var(--border);padding:0.6rem 0;display:flex;align-items:flex-start;gap:0.75rem">
      <div style="flex:1;min-width:0">
        <div style="font-size:0.9rem;font-weight:600;overflow:hidden;text-overflow:ellipsis;white-space:nowrap"
             title="${esc(res.filename)}">${esc(res.filename)}</div>
        <div style="font-size:0.78rem;color:var(--text-muted);margin-top:0.2rem">
          👤 ${esc(res.uploader || '?')} &nbsp;·&nbsp; ↓ ${res.downloads} &nbsp;·&nbsp; ★ ${res.rating}
          ${res.hash_match ? '&nbsp;·&nbsp; <span style="color:var(--success)">hash ✓</span>' : ''}
        </div>
      </div>
      <button class="btn btn-primary btn-sm" id="osbtn_${res.file_id}"
              onclick="osStandaloneConfirm(${res.file_id},'${esc(res.filename)}','${lang}',this)">
        ${t('js_add')}
      </button>
    </div>`).join('');
  _osOpenModal();
}
```

- [ ] **Step 3.5: Aggiorna osStandaloneConfirm (Sync) — non chiude il modale**

Trova la funzione `osStandaloneConfirm` (riga ~4620) e sostituiscila:

```js
async function osStandaloneConfirm(file_id, filename, lang, btn) {
  btn.disabled = true; btn.textContent = '⏳';
  try {
    const r = await fetch('/api/subtitles/download', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ file_id, filename }),
    });
    const data = await r.json();
    if (!r.ok) throw new Error(data.detail || t('js_error'));
    const isoLang = { it:'ita', en:'eng', fr:'fra', de:'deu', es:'spa', pt:'por' }[lang] || lang;
    const uid = -(Date.now() % 1000000);
    S.trackTable.push({
      type: 'subtitle', source: 'source', ffprobe_index: uid,
      codec: 'SRT', mkv_codec: 'S_TEXT/UTF8',
      language: isoLang, title: 'SRT (OS)',
      forced: false, default: false, include: true, delay_ms: 0,
      action: 'ocr', converted_path: data.path,
    });
    renderTrackTable();
    _osIncrementCounter();
    btn.textContent = t('js_added');
    btn.disabled = false;
  } catch (e) {
    btn.disabled = false; btn.textContent = t('js_add');
    alert(t('js_download_failed') + e.message);
  }
}
```

- [ ] **Step 3.6: Aggiorna mxOsStandaloneShowResults (Mux) — modale persistente**

Trova `mxOsStandaloneShowResults` (riga ~4690) e sostituiscila:

```js
function mxOsStandaloneShowResults(results, hash, apiTotal, lang, fileIdx) {
  const body = document.getElementById('osSearchBody');
  if (results.length === 0) {
    const hashInfo = hash ? `<br><small style="font-family:monospace;opacity:.7">hash: ${hash} · api_total: ${apiTotal ?? '?'}</small>` : '';
    body.innerHTML = `<p style="color:var(--text-muted);text-align:center;padding:1rem">${t('js_os_no_results')}${hashInfo}</p>`;
    _osOpenModal();
    return;
  }
  body.innerHTML = results.map(res => `
    <div style="border-bottom:1px solid var(--border);padding:0.6rem 0;display:flex;align-items:flex-start;gap:0.75rem">
      <div style="flex:1;min-width:0">
        <div style="font-size:0.85rem;font-weight:600;word-break:break-all">${esc(res.filename)}</div>
        <div style="font-size:0.78rem;color:var(--text-muted);margin-top:2px">
          ${res.uploader ? `👤 ${esc(res.uploader)}` : ''} ↓ ${res.downloads} · ★ ${res.rating}
          ${res.hash_match ? '· <span style="color:var(--success)">hash ✓</span>' : ''}
        </div>
      </div>
      <button class="btn btn-primary btn-xs" id="mxosbtn_${res.file_id}"
              onclick="mxOsStandaloneConfirm(${res.file_id},'${esc(res.filename)}','${lang}',${fileIdx},this)">
        ${t('js_add')}
      </button>
    </div>`).join('');
  _osOpenModal();
}
```

- [ ] **Step 3.7: Aggiorna mxOsStandaloneConfirm (Mux) — non chiude il modale**

Trova `mxOsStandaloneConfirm` (riga ~4715) e sostituiscila:

```js
async function mxOsStandaloneConfirm(file_id, filename, lang, fileIdx, btn) {
  btn.disabled = true; btn.textContent = '⏳';
  try {
    const r = await fetch('/api/subtitles/download', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ file_id, filename }),
    });
    const data = await r.json();
    if (!r.ok) throw new Error(data.detail || t('js_error'));
    const isoLang = { it:'ita', en:'eng', fr:'fra', de:'deu', es:'spa', pt:'por' }[lang] || lang;
    const uid = -(Date.now() % 1000000);
    MX.tracks.push({
      type: 'subtitle', source_file_idx: fileIdx, ffprobe_index: uid, mkvmerge_id: -1,
      codec: 'SRT', mkv_codec: 'S_TEXT/UTF8',
      language: isoLang, title: 'SRT (OS)',
      forced: false, default: false, include: true, delay_ms: 0,
      action: 'ocr', converted_path: data.path,
    });
    mxRenderTrackTable();
    _osIncrementCounter();
    btn.textContent = t('js_added');
    btn.disabled = false;
  } catch (e) {
    btn.disabled = false; btn.textContent = t('js_add');
    alert(t('js_download_failed') + e.message);
  }
}
```

- [ ] **Step 3.8: Aggiorna anche mxOsShowResults (VobSub azioni) — stessa UI bottone**

Nota: `mxOsConfirmDownload` (VobSub) DEVE ancora chiudere il modale (è un'azione puntuale su una traccia esistente). Aggiorna solo il testo bottone da `t('js_use_this')` a `t('js_add')` nei risultati renderizzati da `mxOsShowResults` (righe ~4500-4530).

Trova in `mxOsShowResults` la riga con `t('js_use_this')`:
```js
        ${t('js_use_this')}
```
Sostituisci con:
```js
        ${t('js_add')}
```

Trova anche in `osStandaloneShowResults` (la vecchia funzione che stai rimpiazzando nel step 3.4) — già gestito.

- [ ] **Step 3.9: Commit**

```bash
git add app/static/index.html app/static/app.js
git commit -m "feat(os): modale risultati persistente per download multipli

- Modale non si chiude dopo Aggiungi (opzione C)
- Contatore 'N sottotitolo/i aggiunto/i' visibile in cima
- ffprobe_index univoco negativo per tracce OS standalone
- mkvmerge_id=-1 esplicito su MX.tracks.push

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Task 4: Mux HTML — wizard 5 step

**Files:**
- Modify: `app/static/index.html` (sezione muxSection, righe ~864-1038)

- [ ] **Step 4.1: Aggiorna il breadcrumb Mux a 5 pill**

Trova la `<nav class="steps-nav" id="muxStepsNav">` e sostituisci il contenuto:

```html
  <nav class="steps-nav" id="muxStepsNav">
    <div class="step-pill active" data-mux-step="1"><span class="step-num">1</span> <span data-i18n="mux_step_file">File</span></div>
    <div class="step-pill" data-mux-step="2"><span class="step-num">2</span> <span data-i18n="mux_step_actions">Azioni</span> <span style="font-size:0.7em;opacity:0.7">(opt.)</span></div>
    <div class="step-pill" data-mux-step="3"><span class="step-num">3</span> <span data-i18n="mux_step_os">OS</span> <span style="font-size:0.7em;opacity:0.7">(opt.)</span></div>
    <div class="step-pill" data-mux-step="4"><span class="step-num">4</span> <span data-i18n="mux_step_tracks">Tracce</span></div>
    <div class="step-pill" data-mux-step="5"><span class="step-num">5</span> <span data-i18n="mux_step_mux">Mux</span></div>
  </nav>
```

- [ ] **Step 4.2: Aggiungi muxStep3 (OS) prima del vecchio muxStep3**

Inserisci il seguente blocco HTML dopo la chiusura di `</div><!-- /muxStep2 -->` e prima di `<!-- ── Step 3: Track table -->`:

```html
  <!-- ── Step 3: OpenSubtitles download (opzionale) ────────────────────── -->
  <div id="muxStep3" class="step-section hidden">
    <button class="btn btn-ghost btn-sm mb-1" onclick="mxGoStep(2)" data-i18n="btn_back">← Indietro</button>
    <div class="card">
      <div class="card-title"><span data-i18n="os_step_title">🌐 Scarica sottotitoli da OpenSubtitles</span></div>
      <div style="display:flex;align-items:center;gap:0.5rem;flex-wrap:wrap;padding:0.5rem 0">
        <label style="font-size:0.8rem;color:var(--text-muted)" data-i18n="os_step_lang_label">Lingua:</label>
        <select id="mxOsLang" style="font-size:0.82rem">
          <option value="it">Italiano</option>
          <option value="en">English</option>
          <option value="fr">Français</option>
          <option value="de">Deutsch</option>
          <option value="es">Español</option>
          <option value="pt">Português</option>
        </select>
        <label style="font-size:0.8rem;color:var(--text-muted)" data-i18n="os_step_file_label">Da file:</label>
        <select id="mxOsFileSel" style="font-size:0.82rem;max-width:200px"></select>
        <button class="btn btn-ghost btn-xs" id="btnMxOsSearch"
                onclick="mxOsStepSearch()" data-i18n="os_step_search_btn">Cerca</button>
        <span id="mxOsStepStatus" style="font-size:0.78rem;color:var(--text-muted)"></span>
      </div>
    </div>
    <div class="step-nav-btns" style="margin-top:0.5rem">
      <div></div>
      <button class="btn btn-primary" onclick="mxGoStep(4)" data-i18n="os_step_skip">Avanti senza scaricare →</button>
    </div>
  </div>
```

- [ ] **Step 4.3: Rinomina il vecchio muxStep3 → muxStep4**

Trova `<div id="muxStep3" class="step-section hidden">` (il vecchio step3, con track table) e cambia l'id:

```html
  <div id="muxStep4" class="step-section hidden">
```

Trova il back button dentro muxStep4 (era `onclick="mxGoStep(1)"`), cambia in:

```html
    <button class="btn btn-ghost btn-sm mb-1" onclick="mxGoStep(3)" data-i18n="btn_back">← Indietro</button>
```

Rimuovi il pannello `mxOsStandalonePanel` (le righe ~926-950 col collassabile OS) — tutte le righe da `<!-- OS1: pannello standalone OpenSubtitles (Mux) -->` fino alla chiusura `</div>` inclusa.

- [ ] **Step 4.4: Rinomina il vecchio muxStep4 → muxStep5**

Trova `<div id="muxStep4" class="step-section hidden">` (il vecchio step4, con progress) e cambia l'id:

```html
  <div id="muxStep5" class="step-section hidden">
```

Trova il pulsante "← Torna alle tracce" nel result card (era `onclick="mxGoStep(2)"`):

```html
        <button class="btn btn-ghost btn-sm" onclick="mxGoStep(4)" data-i18n="mux_back_tracks">← Torna alle tracce</button>
```

- [ ] **Step 4.5: Commit**

```bash
git add app/static/index.html
git commit -m "feat(mux): wizard 5 step con step 3 OS dedicato

Breadcrumb: File > Azioni(opt.) > OS(opt.) > Tracce > Mux
Rimosso pannello OS collassabile dal tab Tracce.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Task 5: Mux JS — navigazione 5 step

**Files:**
- Modify: `app/static/app.js`

- [ ] **Step 5.1: Aggiorna mxGoStep — nessuna modifica alla funzione**

La funzione `mxGoStep` è generica (`muxStep${n}`, data-mux-step): funzionerà automaticamente con 5 step dopo l'aggiunta del pill e dello step in HTML. Nessuna modifica necessaria alla funzione stessa.

- [ ] **Step 5.2: Aggiorna le chiamate mxGoStep — progress**

In `mxStartMux` (riga ~4124), cambia:
```js
    mxGoStep(4);
```
in:
```js
    mxGoStep(5);
```

- [ ] **Step 5.3: Aggiorna mxGoStep(3) in mxAcceptAllActions**

In `mxAcceptAllActions` (riga ~4467):
```js
  mxGoStep(3);
```
rimane uguale — step 3 è ora lo step OS, che è il prossimo step corretto dopo le azioni.

- [ ] **Step 5.4: Aggiorna mxGoStep nel risultato mux**

In `mxReset` (riga ~4238):
```js
  mxGoStep(1);
```
rimane uguale. Il back button nel result card è già aggiornato nell'HTML (Task 4.4).

- [ ] **Step 5.5: Aggiungi funzione mxOsStepSearch**

Aggiungi dopo le funzioni `mxOsStandaloneConfirm` / `toggleMxOsStandalonePanel` (intorno alla riga 4740), la nuova funzione per lo step OS Mux:

```js
/* ── Step 3 OS: ricerca standalone Mux ─────────────────────────────────── */

function mxOsStepUpdateFileSel() {
  const sel = document.getElementById('mxOsFileSel');
  if (!sel) return;
  sel.innerHTML = MX.files.map((f, i) => `<option value="${i}">F${i+1}: ${esc(f.name)}</option>`).join('');
}

async function mxOsStepSearch() {
  let cfg;
  try { const r = await fetch('/api/config'); cfg = (await r.json()).opensubtitles || {}; }
  catch { cfg = {}; }
  if (!cfg.username) {
    _osPendingSearch = () => mxOsStepSearch();
    document.getElementById('osCredsModal').showModal();
    return;
  }
  const lang    = document.getElementById('mxOsLang').value;
  const fileIdx = parseInt(document.getElementById('mxOsFileSel')?.value) || 0;
  const statusEl = document.getElementById('mxOsStepStatus');
  const mkv_path = MX.files[fileIdx]?.path;
  if (!mkv_path) { alert(t('js_error')); return; }
  if (statusEl) statusEl.textContent = t('js_searching');
  try {
    const r = await fetch('/api/subtitles/search', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ file: mkv_path, language: lang }),
    });
    const data = await r.json();
    if (!r.ok) throw new Error(data.detail || t('js_error'));
    if (statusEl) statusEl.textContent = '';
    mxOsStandaloneShowResults(data.results || [], data.hash, data.api_total, lang, fileIdx);
  } catch (e) {
    if (statusEl) statusEl.textContent = '✗ ' + e.message;
  }
}
```

- [ ] **Step 5.6: Chiama mxOsStepUpdateFileSel dopo l'analisi**

In `mxAnalyzeAll` (cerca la riga che chiama `mxGoStep(MX.suggestedActions.length > 0 ? 2 : 3)`, intorno alla riga 3930), aggiungi prima di quella riga:

```js
    mxOsStepUpdateFileSel();
```

- [ ] **Step 5.7: Rimuovi funzioni collassabile vecchie**

Rimuovi le seguenti funzioni da `app.js` (erano per il pannello collassabile rimosso in Task 4):
- `toggleMxOsStandalonePanel()` — funzione di ~6 righe
- `mxOsStandaloneUpdateFileSel()` — funzione di ~5 righe

Le funzioni `mxOsStandaloneSearch` e `mxOsStandaloneConfirm` restano perché usate da `mxOsStepSearch` (via `mxOsStandaloneShowResults`/`mxOsStandaloneConfirm`).

Nota: `mxOsStandaloneSearch` non è più chiamata direttamente — può essere lasciata o rinominata. Lasciala per ora (dead code, verrà rimossa in una sessione di pulizia).

- [ ] **Step 5.8: Commit**

```bash
git add app/static/app.js
git commit -m "feat(mux): JS step 3 OS — mxOsStepSearch + navigazione 5 step

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Task 6: Sync HTML — wizard 7 step

**Files:**
- Modify: `app/static/index.html` (sezione syncSection, righe ~140-600)

- [ ] **Step 6.1: Aggiorna breadcrumb Sync a 7 pill**

Trova la `<nav class="steps-nav" id="stepsNav">` e sostituisci il contenuto:

```html
  <nav class="steps-nav" id="stepsNav">
    <div class="step-pill active" data-step="1"><span class="step-num">1</span> <span data-i18n="step_file">File</span></div>
    <div class="step-pill" data-step="2"><span class="step-num">2</span> <span data-i18n="step_analysis">Analisi</span></div>
    <div class="step-pill" data-step="3"><span class="step-num">3</span> <span data-i18n="step_offset">Offset</span></div>
    <div class="step-pill" data-step="4"><span class="step-num">4</span> <span data-i18n="step_output">Output</span></div>
    <div class="step-pill" data-step="5"><span class="step-num">5</span> <span data-i18n="step_os">OS</span> <span style="font-size:0.7em;opacity:0.7">(opt.)</span></div>
    <div class="step-pill" data-step="6"><span class="step-num">6</span> <span data-i18n="step_tracks">Tracce</span></div>
    <div class="step-pill" data-step="7"><span class="step-num">7</span> <span data-i18n="step_mux">Mux</span></div>
  </nav>
```

- [ ] **Step 6.2: Aggiungi section step5 (OS) prima del vecchio step5**

Inserisci dopo la chiusura di `</section><!-- /step4 -->` e prima di `<!-- ── Step 5: Track table -->`:

```html
  <!-- ── Step 5: OpenSubtitles download (opzionale) ──────────────────── -->
  <section id="step5" class="step-section hidden">
    <div class="card">
      <div class="card-title"><span data-i18n="os_step_title">🌐 Scarica sottotitoli da OpenSubtitles</span></div>
      <div id="osSeasonWarn" style="display:none;padding:0.5rem 0;font-size:0.85rem;color:var(--text-muted)"
           data-i18n="os_step_season_warn">
        In modalità stagione i sottotitoli OS si aggiungono per singolo episodio dopo il batch.
      </div>
      <div id="osStepControls" style="display:flex;align-items:center;gap:0.5rem;flex-wrap:wrap;padding:0.5rem 0">
        <label style="font-size:0.8rem;color:var(--text-muted)" data-i18n="os_step_lang_label">Lingua:</label>
        <select id="osStepLang" style="font-size:0.82rem">
          <option value="it">Italiano</option>
          <option value="en">English</option>
          <option value="fr">Français</option>
          <option value="de">Deutsch</option>
          <option value="es">Español</option>
          <option value="pt">Português</option>
        </select>
        <button class="btn btn-ghost btn-xs" id="btnOsStepSearch"
                onclick="osStepSearch()" data-i18n="os_step_search_btn">Cerca</button>
        <span id="osStepStatus" style="font-size:0.78rem;color:var(--text-muted)"></span>
      </div>
    </div>
    <div class="step-nav-btns">
      <button class="btn btn-ghost" onclick="goToStep(4)" data-i18n="btn_back">← Indietro</button>
      <button class="btn btn-primary" onclick="goToStep(6)" data-i18n="os_step_skip">Avanti senza scaricare →</button>
    </div>
  </section>
```

- [ ] **Step 6.3: Rinomina vecchio step5 → step6**

Trova `<section id="step5" class="step-section hidden">` (il vecchio track table step) e cambia l'id:

```html
  <section id="step6" class="step-section hidden">
```

Nel `singleTrackPanel` dentro step6, il back button era `onclick="goToStep(4)"`. Cambialo in:

```html
        <button class="btn btn-ghost" onclick="goToStep(5)" data-i18n="btn_back">← Indietro</button>
```

Nel `seasonSummaryPanel` dentro step6, il back button era `onclick="goToStep(4)"`. **Rimane** `goToStep(4)` (in season mode si salta l'OS step).

Rimuovi il pannello OS collassabile `<div class="card" id="osStandalonePanel">` (righe ~517-538) con tutto il suo contenuto dalla sezione `singleTrackPanel`.

- [ ] **Step 6.4: Rinomina vecchio step6 → step7**

Trova `<section id="step6" class="step-section hidden">` (progress) e cambia l'id:

```html
  <section id="step7" class="step-section hidden">
```

Trova il bottone "← Torna alle tracce" nel result area (era `onclick="goToStep(5)"`):

```html
        <button class="btn btn-ghost btn-sm" onclick="goToStep(6)" data-i18n="btn_back_tracks">← Torna alle tracce</button>
```

- [ ] **Step 6.5: Aggiorna pulsante "Riepilogo →" in step4 (season mode)**

Trova (riga ~441 originale):
```html
      <button class="btn btn-success hidden" id="btnGoSummary" onclick="goToStep(5)" data-i18n="sz_btn_go_summary">Riepilogo →</button>
```
Cambia in:
```html
      <button class="btn btn-success hidden" id="btnGoSummary" onclick="goToStep(6)" data-i18n="sz_btn_go_summary">Riepilogo →</button>
```

(In season mode saltiamo l'OS step e andiamo direttamente al riepilogo season in step6.)

- [ ] **Step 6.6: Commit**

```bash
git add app/static/index.html
git commit -m "feat(sync): wizard 7 step con step 5 OS dedicato

Breadcrumb: File > Analisi > Offset > Output > OS(opt.) > Tracce > Mux
Rimosso pannello OS collassabile dal tab Tracce.
In season mode btnGoSummary salta step OS -> goToStep(6).

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Task 7: Sync JS — navigazione 7 step

**Files:**
- Modify: `app/static/app.js`

- [ ] **Step 7.1: Aggiorna goToStep — logica step 6 (era step 5)**

Trova la funzione `goToStep` (riga ~875) e aggiorna come segue:

**Prima:**
```js
function goToStep(n) {
  const isSeason = S.mode === 'season';

  if (n === 5 && !isSeason) {
    S.outputDir  = document.getElementById('outputDir').value;
    S.outputName = document.getElementById('outputName').value;
    renderTrackTable();
  }

  // Show/hide season vs single content at step 5
  if (n === 5) {
    document.getElementById('seasonSummaryPanel').classList.toggle('hidden', !isSeason);
    document.getElementById('singleTrackPanel').classList.toggle('hidden', isSeason);
    if (isSeason) renderBatchOffsetSummary();
  }

  _showStep(n);
}
```

**Dopo:**
```js
function goToStep(n) {
  const isSeason = S.mode === 'season';

  if (n === 5) {
    // OS step: mostra/nascondi avviso season, disabilita cerca in season mode
    document.getElementById('osSeasonWarn').style.display = isSeason ? 'block' : 'none';
    document.getElementById('osStepControls').style.display = isSeason ? 'none' : 'flex';
  }

  if (n === 6 && !isSeason) {
    S.outputDir  = document.getElementById('outputDir').value;
    S.outputName = document.getElementById('outputName').value;
    renderTrackTable();
  }

  // Show/hide season vs single content at step 6
  if (n === 6) {
    document.getElementById('seasonSummaryPanel').classList.toggle('hidden', !isSeason);
    document.getElementById('singleTrackPanel').classList.toggle('hidden', isSeason);
    if (isSeason) renderBatchOffsetSummary();
  }

  _showStep(n);
}
```

- [ ] **Step 7.2: Aggiorna le chiamate goToStep(6) → goToStep(7) per il progress**

In `app.js`, cerca tutte le occorrenze di `goToStep(6)` che portano al progress (le 3 righe: ~2082, ~2379, ~2428) e cambia ciascuna in `goToStep(7)`:

```js
    goToStep(7);
```

Verifica: `grep -n "goToStep(6)" app/static/app.js` — deve dare 0 risultati dopo la modifica.

- [ ] **Step 7.3: Aggiungi funzione osStepSearch (Sync)**

Rimuovi le vecchie funzioni `toggleOsStandalonePanel` e `osStandaloneSearch` da `app.js`. Aggiungi la nuova funzione step OS Sync dopo le funzioni OS (intorno alla riga della vecchia `osStandaloneSearch`):

```js
/* ── Step 5 OS Sync: ricerca standalone ────────────────────────────────── */

async function osStepSearch() {
  let cfg;
  try { const r = await fetch('/api/config'); cfg = (await r.json()).opensubtitles || {}; }
  catch { cfg = {}; }
  if (!cfg.username || !cfg.api_key || !cfg.has_password) {
    _osPendingSearch = () => osStepSearch();
    document.getElementById('osCredsModal').showModal();
    return;
  }
  const lang     = document.getElementById('osStepLang').value;
  const statusEl = document.getElementById('osStepStatus');
  const mkv_path = S.videoFile;
  if (!mkv_path) { alert(t('js_error')); return; }
  if (statusEl) statusEl.textContent = t('js_searching');
  try {
    const r = await fetch('/api/subtitles/search', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ file: mkv_path, language: lang }),
    });
    const data = await r.json();
    if (!r.ok) throw new Error(data.detail || t('js_error'));
    if (statusEl) statusEl.textContent = '';
    osStandaloneShowResults(data.results || [], data.hash, data.api_total, lang);
  } catch (e) {
    if (statusEl) statusEl.textContent = '✗ ' + e.message;
  }
}
```

La funzione `osStandaloneConfirm` è già stata aggiornata in Task 3 per usare `ffprobe_index` univoco e non chiudere il modale — può essere richiamata sia da `osStandaloneShowResults` (che era il vecchio flusso collassabile, ora rimosso dall'HTML ma il bottone `osStandaloneConfirm` resta nel render dei risultati — Task 3.4).

- [ ] **Step 7.4: Verifica nessun riferimento ai vecchi ID rimossi**

```bash
grep -n "osStandaloneBody\|osStandaloneToggle\|osStandalonePanel\|toggleOsStandalonePanel\|mxOsStandaloneBody\|mxOsStandaloneToggle\|mxOsStandalonePanel\|toggleMxOsStandalonePanel\|mxOsStandaloneUpdateFileSel\|mxOsStandaloneFileSel\|mxOsStandaloneStatus" app/static/app.js app/static/index.html
```

Atteso: nessun risultato (o solo commenti). Se ci sono riferimenti, rimuovili.

- [ ] **Step 7.5: Commit**

```bash
git add app/static/app.js
git commit -m "feat(sync): JS step 5 OS — osStepSearch + goToStep 7 step

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

---

## Task 8: Verifica finale

- [ ] **Step 8.1: Esegui tutti i test unitari**

```bash
cd /media/devmon/Esterno1/Samba/mkv_maximus
python -m unittest tests/test_unit.py -v 2>&1 | tail -10
```

Atteso: `OK` — tutti i test passano (incluso `TestSimpleMuxTrack`).

- [ ] **Step 8.2: Verifica breadcrumb JS breadcrumb Sync**

```bash
grep -n "data-step=" app/static/index.html | head -20
```

Atteso: 7 pill con data-step da 1 a 7, poi le righe `<section id="step1">` … `<section id="step7">`.

- [ ] **Step 8.3: Verifica breadcrumb Mux**

```bash
grep -n "data-mux-step=" app/static/index.html
```

Atteso: 5 pill con data-mux-step da 1 a 5, poi `muxStep1` … `muxStep5`.

- [ ] **Step 8.4: Verifica nessun goToStep(6) rimasto per progress**

```bash
grep -n "goToStep(6)" app/static/app.js app/static/index.html
```

Atteso: solo il back button `seasonSummaryPanel` in index.html (che è corretto — back da step6 season → step4). Nessun risultato in app.js.

- [ ] **Step 8.5: Verifica nessun mxGoStep(4) rimasto per progress**

```bash
grep -n "mxGoStep(4)" app/static/app.js app/static/index.html
```

Atteso: il back button in muxStep5 result card `mxGoStep(4)` (torna alle tracce — corretto). Nessuna chiamata al progress con step 4.

- [ ] **Step 8.6: Test manuale nel browser**

Apri `http://192.168.1.76:7788`:

**Sync — singolo film:**
1. Seleziona file video e audio/sub → Analizza tracce →
2. Configura offset → Calcola →
3. Imposta output →
4. Step 5 OS: verifica che appaia il pannello con "Cerca" e "Avanti senza scaricare →"
5. Cerca un film con credenziali OS → modale persistente → Aggiungi 2 sub → verifica contatore
6. "Avanti senza scaricare →" → step 6 Tracce con righe OS visibili
7. Avvia mux → step 7 progress

**Sync — season mode:**
1. Seleziona cartelle → modalità stagione
2. Procedi fino a step 4 Output → "Riepilogo →" → verifica che salti step 5 e vada a step 6 (season summary)

**Mux:**
1. Aggiungi file → Analizza → step 1 → step 2 azioni → step 3 OS
2. Verifica breadcrumb a 5 pill
3. Cerca e aggiungi 2 sub → contatore → "Avanti senza scaricare →" → step 4 Tracce
4. Verifica righe OS nella tabella con mkvmerge_id=-1 (non visibile ma non deve dare errore)
5. Avvia mux → step 5 progress → verifica che il mux con SRT OS non dia `[object Object]`

- [ ] **Step 8.7: Commit finale e aggiornamento CLAUDE.md**

```bash
git add -p  # verifica tutto
git commit -m "chore: verifica completata B18 OS step

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
```

Aggiorna la sezione "Stato implementazione" in `CLAUDE.md` con:
```
B18 ✅: FEATURE-OS-DOWNLOAD (step 5 Sync + step 3 Mux), BUG-OS-MULTI ([object Object] fix), BUG-OS-LANG (ffprobe_index univoco), modale persistente multi-download (2026-04-20)
```

---

## Self-Review Checklist

**Spec coverage:**
- ✅ BUG-OS-MULTI: Task 1 (mkvmerge_id default) + Task 3.7 (mkvmerge_id esplicito nel push)
- ✅ BUG-OS-LANG: Task 3.5/3.7 (ffprobe_index univoco)
- ✅ FEATURE-OS-DOWNLOAD (Mux step 3): Task 4 + 5
- ✅ FEATURE-OS-DOWNLOAD (Sync step 5): Task 6 + 7
- ✅ Multi-download modale persistente (opzione C): Task 3
- ✅ Season mode warning, skip OS step: Task 6.5 + 7.1
- ✅ FEATURE-OS-REMOVE: non richiede codice (checkbox Includi già funzionante dopo B17)

**Nessun placeholder** — tutto il codice è completo.

**Consistenza tipi:**
- `osStandaloneConfirm` usa `osStandaloneShowResults` che chiama `osStandaloneConfirm` (ciclo corretto nel render HTML)
- `mxOsStandaloneConfirm` usa `mxOsStandaloneShowResults` che chiama `mxOsStandaloneConfirm` (corretto)
- `mxOsStepSearch` chiama `mxOsStandaloneShowResults` (riuso corretto)
- `osStepSearch` chiama `osStandaloneShowResults` (riuso corretto)
