# Copy this file to .env and set your media path.
# .env is in .gitignore — it is never overwritten by git pull.

MEDIA_PATH=/media/youruser/HDD

# Multiple drives: add extra volume entries in docker-compose.yml instead.
